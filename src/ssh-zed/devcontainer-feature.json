{
  "id": "ssh-zed",
  "version": "1.0.2",
  "name": "OpenSSH Server for Zed",
  "description": "Configures sshd on port 2222 for Zed remote development. Requires openssh-server in the base image. The devcontainer runArgs must include --cap-add=SETUID and --cap-add=SETGID."
}
