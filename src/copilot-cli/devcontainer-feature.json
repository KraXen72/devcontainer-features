{
  "id": "copilot-cli",
  "version": "1.4.0",
  "name": "GitHub Copilot CLI",
  "description": "Installs GitHub Copilot CLI from the official release installer.\nSettings are shared across all containers via a named Docker volume (copilot-shared).",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Copilot CLI version to install. Use 'latest', 'prerelease', or a specific version like '1.2.3' or 'v1.2.3'."
    },
    "autoUpdate": {
      "type": "boolean",
      "default": true,
      "description": "Run 'copilot update' on each container start. Only useful when version is 'latest' or 'prerelease'."
    }
  },
  "mounts": [
    {
      "source": "copilot-shared",
      "target": "/home/vscode/.copilot",
      "type": "volume"
    }
  ],
  "postStartCommand": "mkdir -p /home/vscode/.copilot && [ -f /home/vscode/.copilot/settings.json ] || echo '{}' > /home/vscode/.copilot/settings.json; [ -f /etc/devcontainer-copilot-cli/auto-update ] && copilot update --yes 2>/dev/null || true"
}
