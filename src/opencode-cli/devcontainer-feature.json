{
  "id": "opencode-cli",
  "version": "1.0.0",
  "name": "OpenCode CLI",
  "description": "Installs the OpenCode CLI.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version to install (e.g. '1.17.4')."
    }
  }
}
