{
  "id": "opencode-cli",
  "version": "1.2.0",
  "name": "OpenCode CLI",
  "description": "Installs the OpenCode CLI.\nOpenCode state (sessions, config, auth) is stored in a persistent container-side named volume.\nWhen launched through dcman with the 'opencode' preset, dcman injects OPENCODE_CONFIG=~/.config/opencode/opencode-yolo.json to run in yolo (auto-approve) mode.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version to install (e.g. '1.17.4')."
    }
  },
  "mounts": [
    {
      "source": "opencode-shared",
      "target": "/home/vscode/.opencode-shared",
      "type": "volume"
    }
  ]
}
